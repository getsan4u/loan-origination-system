'use strict';

const new_api_credentials = require('./new_api_credentials');
const new_org = require('./new_org');
const new_password = require('./new_password');
const new_user = require('./new_user');
const profile = require('./profile');

module.exports = {
  new_api_credentials,
  new_org,
  new_password,
  new_user,
  profile,
};