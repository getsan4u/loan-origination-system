'use strict';

const component = require('./component');
const props = require('./props');
const form_creator = require('./form_creator');

module.exports = {
  component,
  props,
  form_creator,
};