'use strict';
const utilities = require('../../../../utilities');
const { edit } = utilities.views.decision.shared.manifests.edit;
const { editArr } = utilities.views.decision.shared.components.manifestConfigs.configArr;
const util = require('util');
// console.log(util.inspect(editArr, {depth: 20}));
// module.exports = editArr.map(edit);